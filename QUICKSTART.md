# 快速開始指南

## ⚡ 5 分鐘快速上手

### 1. 打開儀表板

直接在瀏覽器中打開 `index.html` 文件：

```bash
# 方法 A：直接打開
open index.html

# 方法 B：使用本地服務器（推薦）
python -m http.server 8000
# 然後在瀏覽器中訪問 http://localhost:8000
```

### 2. 瀏覽儀表板

- **向下滾動**查看 5 個部分
- **點擊分類按鈕**快速導航
- **點擊節點**選擇不同狀態
- **點擊"展開全文"**查看詳細說明

### 3. 加載您的數據

#### 準備 CSV 文件

創建一個 CSV 文件（例如 `companies.csv`）：

```csv
公司,統一編號,是否承諾淨零排放或碳中和,預計達成淨零排放年份,是否設定中期溫室氣體絕對減量目標,中期減量目標年設定,中期溫室氣體絕對減量目標值（百分比）,中期目標是否取得SBT認證,是否取得RE100認證,是否揭露2022-2024年溫室氣體排放資料
台灣鋼鐵集團,12345678,納入策略,2050,已設定,2030,30,已認證,未加入,完整
台灣能源公司,87654321,承諾,2060,未設定,,,未認證,已加入,部分
```

#### 配置路徑

編輯 `data.js` 文件，找到以下行：

```javascript
// 大約在第 465 行
const CSV_PATH = '/path/to/companies.csv'; // ← 更改此路徑
```

改為：

```javascript
const CSV_PATH = '/companies.csv'; // 相對於 HTML 文件
```

#### 加載數據

重新加載頁面。儀表板將自動：
- 加載 CSV 數據
- 解析公司信息
- 填充所有指標
- 顯示圖表和趨勢

---

## 🎨 常用定制

### 修改公司名稱

儀表板會自動從 CSV 的"公司"列讀取。無需額外操作。

### 改變顏色主題

編輯 `styles.css` 中的顏色變量（約第 15-20 行）：

```css
:root {
    --color-section-commitments: #0066CC;   /* 藍色 */
    --color-section-emissions: #7030A0;     /* 紫色 */
    --color-section-energy: #00B050;        /* 綠色 */
    --color-section-performance: #FF8C00;   /* 橙色 */
    --color-section-strategy: #E81B23;      /* 粉紅色 */
}
```

### 調整字體大小

編輯 `styles.css` 中的字體大小：

```css
:root {
    --font-size-base: 14px;  /* 改為 16px 使文字更大 */
}
```

### 修改間距

編輯 `styles.css` 中的間距：

```css
:root {
    --spacing-unit: 8px;  /* 改為 10px 增加所有間距 */
}
```

---

## 🔧 故障排除

### 問題：頁面加載，但數據不顯示

**解決方案**：
1. 打開瀏覽器開發者工具（F12）
2. 查看控制台中的錯誤信息
3. 驗證 CSV 文件路徑正確
4. 確保 CSV 文件可訪問（無 CORS 錯誤）

### 問題：節點不可點擊

**解決方案**：
1. 刷新頁面
2. 檢查 script.js 是否已加載
3. 查看控制台中是否有 JavaScript 錯誤

### 問題：圖表空白

**解決方案**：
1. 檢查 charts.js 是否已加載
2. 驗證 CSV 中有數值數據
3. 檢查數據格式是否正確

### 問題：在手機上佈局不正確

**解決方案**：
1. 清除瀏覽器緩存
2. 使用隱私/無痕模式
3. 確保設置了 viewport meta 標籤（已在 HTML 中）

---

## 📊 常用數據操作

### 查看當前加載的公司

在瀏覽器控制台中輸入：

```javascript
dataManager.getCompanyNames()
```

### 加載特定公司

```javascript
dataManager.loadCompanyByName('台灣鋼鐵集團')
```

### 導出當前公司數據

```javascript
// 導出為 JSON
const json = dataManager.exportCompanyData('json')
console.log(json)

// 導出為 CSV
const csv = dataManager.exportCompanyData('csv')
console.log(csv)
```

### 查看特定指標值

```javascript
const company = dataManager.currentCompany
const metric = company.getMetric('net-zero-status')
console.log(metric.value)      // 原始值
console.log(metric.formatted)  // 格式化值
```

---

## 🌐 部署到網絡

### 方法 1：使用 GitHub Pages

1. 將檔案推送到 GitHub 倉庫
2. 設置 GitHub Pages（Settings → Pages）
3. 選擇分支和文件夾
4. 訪問 `https://yourusername.github.io/repo-name`

### 方法 2：使用 Netlify

1. 將檔案推送到 GitHub
2. 在 Netlify 中連接倉庫
3. 自動部署
4. 訪問提供的 URL

### 方法 3：傳統虛擬主機

1. 通過 FTP 上傳所有檔案
2. 確保 CSV 文件也已上傳
3. 更新 data.js 中的 CSV 路徑
4. 訪問您的域名

---

## 📱 移動優化

儀表板已完全優化用於移動設備：

✅ 自動調整佈局
✅ 優化的觸摸區域
✅ 快速加載
✅ 縮放友好

**建議**：
- 使用 Chrome DevTools 測試響應式設計
- 調整 `styles.css` 中的媒體查詢以微調移動體驗

---

## 🔐 安全性考慮

### CSV 數據安全

如果 CSV 包含敏感數據：

1. **使用 API 而不是 CSV**
   - 在 data.js 中用 API 調用替代 CSV 加載
   - 實現服務器端身份驗證

2. **加密敏感字段**
   - 在傳輸前加密
   - 在客戶端解密

3. **限制訪問**
   - 使用密碼保護
   - 實現角色基的訪問控制

### 關鍵檔案

確保這些檔案不可在網絡上訪問：
- ❌ 原始數據文件（使用 API）
- ❌ 密鑰或憑證（使用環境變量）
- ❌ 內部文檔

---

## 📈 性能優化

### 減小檔案大小

```bash
# 縮小 CSS
npm install -g cssnano
cssnano styles.css > styles.min.css

# 縮小 JavaScript
npm install -g terser
terser script.js > script.min.js
```

### 啟用 GZIP 壓縮

服務器配置（.htaccess）：

```apache
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE application/javascript
</IfModule>
```

### 使用 CDN

為大型文件或多地區用戶部署考慮 CDN：
- CloudFront（AWS）
- Cloudflare
- Akamai

---

## 🚀 進階使用

### 集成 Google Analytics

在 `index.html` 的 `<head>` 中添加：

```html
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### 集成 Slack 通知

在 data.js 中添加 Webhook：

```javascript
// 當公司數據加載時
async notifySlack(companyName) {
  await fetch('YOUR_SLACK_WEBHOOK_URL', {
    method: 'POST',
    body: JSON.stringify({
      text: `Loaded data for ${companyName}`
    })
  })
}
```

### 自定義主題

創建新的 CSS 變量集合：

```css
.theme-dark {
  --color-gray-dark: #FFFFFF;
  --color-white: #1a1a1a;
  /* 等等 */
}
```

---

## 📚 更多幫助

- **詳細設計**：查看 `plan.md`
- **功能說明**：查看 `FEATURES.md`
- **數據集成**：查看 `DATA_INTEGRATION_GUIDE.md`
- **完整項目**：查看 `IMPLEMENTATION_SUMMARY.md`

---

## 💡 提示和技巧

### 快速測試數據

使用示例 CSV 進行測試：

```csv
公司,統一編號,是否承諾淨零排放或碳中和,預計達成淨零排放年份,2022年度總能源使用量,2023年度總能源使用量,2024年度總能源使用量
測試公司,99999999,納入策略,2050,1250000,1235000,1210000
```

### 調試模式

在 script.js 中啟用詳細日誌：

```javascript
// 修改 console.log 以包含時間戳
const debugLog = (msg) => console.log(`[${new Date().toLocaleTimeString()}] ${msg}`)
```

### 離線模式

使用 Service Worker 啟用離線功能：

```javascript
// 在 script.js 中
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('sw.js')
}
```

---

## 🎓 學習資源

- [MDN HTML 文檔](https://developer.mozilla.org/en-US/docs/Web/HTML)
- [MDN CSS 文檔](https://developer.mozilla.org/en-US/docs/Web/CSS)
- [MDN JavaScript 文檔](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
- [WCAG 無障礙指南](https://www.w3.org/WAI/WCAG21/quickref/)

---

## ✅ 檢查清單

在部署前確保：

- [ ] 所有檔案都已上傳
- [ ] CSV 文件已放在正確位置
- [ ] data.js 中的路徑已更新
- [ ] 在多個瀏覽器中測試
- [ ] 在移動設備上測試
- [ ] 檢查控制台中沒有錯誤
- [ ] 驗證所有圖表正確顯示
- [ ] 測試所有交互功能
- [ ] 檢查無障礙特性
- [ ] 優化圖像和資源

---

## 🎉 準備就緒！

您現在可以開始使用企業氣候數據儀表板！

**下一步**：
1. 準備您的 CSV 數據
2. 配置路徑
3. 部署到服務器
4. 邀請用戶

**有問題？**查看詳細文檔或在代碼中添加註釋。

祝您使用愉快！🚀

---

**最後更新**：2025年11月14日
